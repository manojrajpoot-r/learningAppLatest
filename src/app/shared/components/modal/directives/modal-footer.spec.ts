import { ModalFooter } from './modal-footer';

describe('ModalFooter', () => {
  it('should create an instance', () => {
    const directive = new ModalFooter();
    expect(directive).toBeTruthy();
  });
});
